# 快计 · 功能扩展改造指南

> 对照你的扩展需求清单，逐项给出「在哪里改 / 在哪里加」。已实现的功能已标 ✔ 忽略。
> 通用前提：本项目所有数据操作统一走双模式数据层 `src/composables/useFinance.js`（guest → localStorage，user → Supabase），任何新数据字段都要在这三个地方同步落地：
> 1. `supabase/schema.sql`（云端表结构 + RLS）
> 2. `src/lib/storage.js`（游客本地结构）
> 3. `src/composables/useFinance.js`（状态 + CRUD，user/guest 各写一套）

---

## 〇、现状盘点（已实现，忽略）

| 需求 | 状态 | 现有位置 |
|---|---|---|
| 收支趋势折线图（每日/每月） | ✔ 已实现 | HomeView 近 30 天折线 + 近 12 个月柱状 |
| 移动端适配 | ✔ 已实现 | App.vue + AppSidebar/Header/Tabbar，断点 lg |
| 自定义收支分类 | ✔ 已实现 | SettingsView（新增/改名/删除） |
| 记账备注字段 | ✔ 已实现 | RecordView note 输入框 |
| 月度总预算 + 首页进度条/超支标红 | ✔ 已实现 | SettingsView 设置 + HomeView 卡片 |
| 重置数据 | ✔ 已实现 | SettingsView |
| RLS 权限隔离 | ✔ 已实现 | schema.sql 12 条策略 |

---

## 一、时间维度切换（单月 → 多维度）

### 1.1 按日 / 周 / 月 / 季 / 年查看收支

- **改 `src/views/HomeView.vue`**：
  - 在月份选择器旁加一个粒度切换按钮组（日 / 周 / 月 / 季 / 年），用 `ref` 存 `granularity`
  - 现在统计口径全写死在 `monthBills`（按 `record_date.startsWith(month)` 过滤）。把它抽成函数：`filterByRange(bills, granularity, anchor)`，按粒度生成日期区间（如"本周"= 周一~周日，"本季"= 当季首末，年同理）
  - 卡片（收入/支出/结余/笔数）、饼图、趋势图全部改为吃这个过滤结果
- **建议（可选）**：把统计逻辑抽成新文件 `src/composables/useStats.js`，放 `filterByRange`、`sumByType`、`groupByMonth` 等纯函数，HomeView 变薄

### 1.2 跨月份对比（8 月 vs 9 月）

- **改 `HomeView.vue`**：加一个"对比"开关 + 两个月份选择器；对比卡片显示两月各自的收入/支出/结余及差额（Δ）
- 数据直接用现有 `bills`，按月份分组求和，无需动数据层
- 建议抽到 `useStats.js`：`compareMonths(bills, m1, m2)`

### 1.3 日期快捷筛选（自定义起止时间）

- **改 `src/views/TransactionsView.vue` 筛选栏**：在现有 月份/类型/分类 下加"起止日期"两个 `<input type="date">`
- `filteredBills` computed 增加区间判断：`b.record_date >= start && b.record_date <= end`
- 月份筛选和起止日期二选一即可（选区间时月份失效），交互上做个互斥

---

## 二、标签功能（与分类区分）

设计：分类=大类别（餐饮/交通），标签=小标记（#外卖 #聚餐 #通勤），一笔可打多个标签。

### 2.1 数据结构

- **改 `supabase/schema.sql`**，`bills` 表加数组列（Postgres 原生数组，Supabase 直接支持）：

```sql
alter table public.bills add column if not exists tags text[] not null default '{}';
```

> RLS 不用改（表级策略已覆盖新列）。游客侧 `storage.js` 的 bills 对象天然可带 `tags: []`，不用改结构代码。

- **改 `src/composables/useFinance.js`**：
  - `addBill` / `updateBill` 的 insert/update 对象加 `tags` 字段
  - `loadFromSupabase` 的 `normalizeBill` 兼容 `tags` 缺失（`row.tags ?? []`）

### 2.2 前端

- **改 `RecordView.vue`**：备注下方加"标签"输入（输入后回车变成一个个小徽章，可删除）
- **改 `TransactionsView.vue`**：
  - 筛选栏加"按标签筛选"（下拉列出所有用过的标签，或输入框）
  - 账单列表行内显示标签徽章（`#外卖` 样式）
  - 编辑弹窗补标签编辑

---

## 三、账户管理（钱包账户 + 资产总览）

### 3.1 数据结构

- **改 `supabase/schema.sql`**，新表：

```sql
create table if not exists public.accounts (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references auth.users (id) on delete cascade,
  name        text not null,            -- 现金 / 微信 / 支付宝 / 银行卡
  icon        text not null default '',  -- 图标标识
  balance     numeric(12,2) not null default 0,
  sort_order  integer not null default 0,
  created_at  timestamptz not null default now()
);
-- 注册触发器内追加：自动种 4 个默认账户（现金/微信/支付宝/银行卡，余额 0）
```

- **改 `storage.js`**：空结构加 `accounts: []`
- **改 `useFinance.js`**：加 `state.accounts` + `accounts` computed + `addAccount / updateAccountBalance / deleteAccount`（user 走 supabase、guest 走 persistLocal）

### 3.2 前端

- **改 `RecordView.vue`**：表单加"账户"选择（支出=从哪扣，收入=存进哪）
- **改 `useFinance.js` 的 `addBill`**：记账成功后同步改账户余额（`account.balance -= amount` / `+= amount`）
- **改 `SettingsView.vue`**：加"账户管理"卡片：各账户余额列表（可改余额）、新增账户、总资产合计
- **改 `HomeView.vue`**：加"资产总览"卡片：`总资产 = Σ 账户余额`，展示微信 3000 / 银行卡 5000 这种明细

---

## 四、导入导出 + 备份恢复

### 4.1 新文件

- **新增 `src/lib/csv.js`**：`parseCSV(text)`（解析带引号转义）、`buildCSV(rows)`（生成）、`downloadFile(name, content, mime)`（触发浏览器下载）

### 4.2 导出

- **改 `TransactionsView.vue`**：右上角加"导出 CSV"按钮 → 当前筛选结果生成 CSV（表头：日期/类型/分类/金额/备注/标签）下载
- Excel 兼容：CSV 用 Excel 能直接打开；若必须 .xlsx 再 `npm install xlsx`（毕设建议 CSV 即可）

### 4.3 导入

- **改 `TransactionsView.vue` 或 SettingsView**：加"导入 CSV"文件选择器 → `parseCSV` → 逐行校验（金额、类型、分类）→ 批量调 `addBill`（自动走当前模式）
- 表头约定写进 README，样例模板一起提供

### 4.4 备份 / 恢复（游客模式重点）

- **改 `SettingsView.vue` 数据管理区**：
  - "导出备份"：把 `{ bills, categories, budgets, accounts }` 打包成 JSON 下载（游客登录用户都能用）
  - "导入备份"：文件选择 → 校验结构 → 覆盖写回 `useFinance` 对应状态并持久化

---

## 五、分类预算 + 80% 预警

现有"月度总预算"已实现；新增"每个分类各自的预算"。

### 5.1 数据结构

- **改 `supabase/schema.sql`**，新表：

```sql
create table if not exists public.category_budgets (
  id        uuid primary key default gen_random_uuid(),
  user_id   uuid not null references auth.users (id) on delete cascade,
  month     text not null,          -- 'YYYY-MM'
  category  text not null,          -- 分类名
  amount    numeric(12,2) not null check (amount >= 0),
  unique (user_id, month, category)
);
```

- **改 `storage.js` / `useFinance.js`**：加 `categoryBudgets` 状态（结构 `{ 'YYYY-MM|餐饮': 金额 }`）与 CRUD

### 5.2 前端

- **改 `SettingsView.vue`**：支出分类列表每个分类行内加预算输入框（按月），下方直接显示该分类进度条
- 进度条规则：`已花/预算`；≥80% 黄色预警（"已用 82%"），≥100% 红色（"超支 ¥xx"），样式复用首页 budget 卡片的写法
- **改 `HomeView.vue`（可选）**：超支/预警的分类在首页也标红提示

---

## 六、收支排行榜

- **改 `HomeView.vue`**：在饼图下方加一张"本月消费 TOP"卡片
  - TOP 分类：`pieData` 前 5 名，列表形式（名次/分类/金额/占比条）
  - TOP 账单：`monthBills` 按金额排序前 5 条（分类+备注+金额）
- 纯 computed，不动数据层

---

## 七、年度汇总看板

- **改 `HomeView.vue`**：新增"年度"区块（或并入 1.1 的"年"粒度视图）
  - 顶部：全年总收入 / 总支出 / 年度结余
  - 主体：12 个月小卡片网格，每月显示"收 / 支 / 结余"，超支月标红
- 数据：`bills` 按 `record_date.slice(0,7)` 分组，纯 computed
- 建议抽到 `useStats.js`：`annualSummary(bills, year)`

---

## 八、快捷记账（模板一键填充）

- **改 `RecordView.vue`**：表单上方加"常用模板"横排按钮（早餐 ¥8 / 通勤 ¥3 / 午餐 ¥20 / 房租…）
- 点击 → 一键填充金额/分类/备注/标签 → 用户确认保存
- 模板数组可先硬编码在 `RecordView.vue` 顶部常量；进阶可存 `localStorage` 让用户自配

---

## 九、记账上传凭证图片

- **改 `supabase/schema.sql`**：`bills` 加 `receipt_url text default ''`
- **登录用户**：Supabase Storage 建 bucket `receipts`（公开读 + 按 user 的 RLS），`RecordView` 加文件选择 → 上传拿 URL → 存进 `receipt_url`
- **游客用户（可砍）**：图片转 base64 存 localStorage 容量有限，建议游客仅显示"未支持"，或限制图片 ≤200KB
- **改 `RecordView.vue`**：备注旁加"凭证"上传区 + 预览缩略图

---

## 十、账单搜索

- **改 `TransactionsView.vue` 筛选栏**：加搜索框
  - 匹配规则：备注包含 / 分类等于 / 金额相等（或输入"50"匹配 50.00）/ 标签包含任一
- `filteredBills` computed 追加 `searchText` 判断，纯前端过滤
- 与标签筛选（2.2）合并进同一筛选区

---

## 十一、暗色 / 亮色主题

- **改 `tailwind.config.js`**：`darkMode: 'class'`
- **新增 `src/composables/useTheme.js`**：`theme` 状态（'light'|'dark'），持久化 localStorage，切换时给 `<html>` 加/去 `dark` class
- **改 `App.vue`**：挂主题初始化；SettingsView 加切换开关
- **改所有组件**：给主要容器加 `dark:` 前缀样式（这是最费工的活，建议放到最后，先做"能切 + 主要页面能用"再逐页补）

---

## 十二、设置页增强

| 子项 | 改哪里 |
|---|---|
| 昵称 / 个人信息 | 登录用户：`supabase.auth.updateUser` 只支持改邮箱密码；昵称建议新建 `profiles` 表（user_id, nickname, avatar_url）或先存 localStorage；游客直接存 localStorage |
| 自定义头像 | 上传 → Supabase Storage（登录）或 base64/URL（游客）→ 存 `avatar_url` → Sidebar/Header 头像组件读取显示 |
| 货币单位 | 改 `src/lib/utils.js` 的 `fmtMoney`：符号从 `¥` 改为可配置（如 `currencySymbol`），全局状态存 localStorage（`useSettings.js` 或并入 useTheme.js） |
| 重置数据 | ✔ 已有 |

---

## 十三、重复账单（周期记账）

### 13.1 数据结构

- **改 `supabase/schema.sql`**，新表：

```sql
create table if not exists public.recurring_bills (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references auth.users (id) on delete cascade,
  type        text not null check (type in ('income','expense')),
  category    text not null,
  amount      numeric(12,2) not null check (amount > 0),
  note        text default '',
  tags        text[] not null default '{}',
  account     text default '',
  frequency   text not null check (frequency in ('day','week','month')),
  next_date   date not null,
  active      boolean not null default true,
  created_at  timestamptz not null default now()
);
```

- **改 `storage.js` / `useFinance.js`**：加 `recurringBills` 状态 + CRUD

### 13.2 生成逻辑（核心）

- **改 `useFinance.js`**：加 `checkRecurring()` —— 遍历到期（`next_date <= today`）且 `active` 的重复账单 → 调 `addBill` 生成真实记录 → `next_date` 按频率 +1 天/周/月 → 更新
- **改 `main.js` 或 `useAuth.js` 的 `initAuth`**：应用启动 / 登录后调用 `checkRecurring()`
- **改 `RecordView.vue`**：加"设为重复"开关 + 频率/下次日期选择

---

## 十四、AI 记账（毕设可做规则版）

### 14.1 一句话记账（规则解析版）

- **改 `RecordView.vue`**：加"文字记账"输入框，如"今天吃饭花了50"
- **新增 `src/lib/parseText.js`**：
  - 正则提金额：`/(\d+(\.\d{1,2})?)/`
  - 关键词→分类映射：`{ 吃饭/早餐/午餐/晚餐: '餐饮', 地铁/公交/打车: '交通', 房租: '房租', 工资: '工资' … }`
  - 命中分类 → 自动填充表单
- 真 AI 版需接第三方 API（如通义/豆包开放平台），毕设演示规则版足够

### 14.2 OCR 识别小票

- 需要 OCR API（收费/申请 key），或简化为：上传小票图 → 手动确认金额分类。标注为"演示版"即可

---

## 十五、债务模块（应收 / 应付）

- **改 `supabase/schema.sql`**，新表：

```sql
create table if not exists public.debts (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references auth.users (id) on delete cascade,
  direction   text not null check (direction in ('borrow','lend')), -- borrow=欠人 lend=借出
  name        text not null,        -- 对方
  amount      numeric(12,2) not null check (amount > 0),
  note        text default '',
  status      text not null default 'pending',  -- pending 未结清 / settled 已结清
  created_at  timestamptz not null default now()
);
```

- **改 `storage.js` / `useFinance.js`**：加 `debts` 状态 + CRUD
- **新增 `src/views/DebtView.vue`**：借入/借出列表、结清操作
- **改 `src/router/index.js`** + Sidebar/Tabbar：加路由和入口
- **改 `HomeView.vue`（可选）**：应收/应付合计小卡片

---

## 十六、报表 PDF 下载

- **方案 A（简单推荐）**：HomeView 加"导出月度报告 PDF"按钮 → `window.print()` + 打印样式（`@media print` 隐藏导航只留报告区），浏览器另存 PDF，零依赖
- **方案 B（真 .pdf 文件）**：`npm install jspdf`，把月度统计数据画进 PDF 下载
- 毕设推荐方案 A，演示流畅、代码量小

---

## 十七、多账本（个人 / 家庭）

- **改 `supabase/schema.sql`**：新表 `ledgers`（id, user_id, name, created_at）+ `bills/categories/budgets` 加 `ledger_id` 列
- 所有查询加 `ledger_id` 过滤；切换账本 = 全局状态换值 → 重载数据
- **工作量大，涉及全部视图**，建议放最后，或答辩只展示设计说明不落地

---

## 十八、游客 → 登录 数据合并

- **改 `useAuth.js` 的 `signIn` 成功分支**（或 LoginView 登录成功后调用）：
  1. 读本地 `loadLocalData()`（游客数据）
  2. 逐条与云端比对去重（同一天+同金额+同分类+同备注 = 视为同一条，跳过）
  3. 剩余本地账单批量 `addBill` 写入云端
  4. 写入成功后 `clearLocalData()` 清本地，切 user 模式
- **新增 `src/composables/useMerge.js`**：`mergeLocalToCloud()` 实现上述逻辑，LoginView 登录成功后调用

---

## 十九、密码重置

- **改 `LoginView.vue`**：登录卡片加"忘记密码？"链接 + 输入邮箱确认弹窗
- **改 `useAuth.js`**：加

```js
export async function resetPassword(email) {
  return supabase.auth.resetPasswordForEmail(email, {
    redirectTo: window.location.origin + '/login'
  })
}
```

- Supabase 控制台 → Authentication → URL Configuration：把重定向 URL 加进白名单

---

## 二十、数据软删除（回收站）

- **改 `supabase/schema.sql`**：`bills` 加 `deleted_at timestamptz`（默认 NULL）
- **改 `useFinance.js`**：
  - `deleteBill`：改为 `update { deleted_at: now() }`（user）/ 本地标记（guest）
  - `loadFromSupabase`：查询加 `.is('deleted_at', null)` 过滤
- **恢复**：SettingsView 或新"回收站"区块：查 `deleted_at` 非空 → 清空 `deleted_at` 恢复
- 与现有"清空当月"并存时注意：清空=物理删 还是 软删，口径写清楚

---

## 优先级建议（毕设答辩路线）

| 批次 | 功能 | 理由 |
|---|---|---|
| P0（先做，性价比最高） | 标签、账户管理、导出 CSV、分类预算预警、账单搜索 | 改动集中、演示直观、工作量小 |
| P1 | 时间维度切换、年度看板、排行榜、快捷记账、游客登录合并 | 丰富统计维度，答辩亮点 |
| P2 | 备份恢复、主题切换、重复账单、密码重置、软删除 | 安全/体验向 |
| P3（可砍/展示设计） | AI 记账、OCR、债务、PDF、多账本 | 工作量大的加分项，时间紧可不落地 |

> 每完成一个模块，`npm run build` 确认零警告；动数据库结构后，`supabase/schema.sql` 整段在 SQL Editor 重跑（幂等，不会报错）。
